# Task Instructions

Implement `crawl_with_scraper` in `repo/scraper.py`.

```python
def crawl_with_scraper(url: str) -> list[str]:
    ...
```

## Constraints
- MIT/Apache/BSD. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
