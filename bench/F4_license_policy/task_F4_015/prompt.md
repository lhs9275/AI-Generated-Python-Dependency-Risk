# Task Instructions

Implement `convert_pdf_to_image` in `repo/pdf2img.py`.

```python
def convert_pdf_to_image(pdf_bytes: bytes, page: int = 0) -> bytes:
    ...
```

## Constraints
- MIT/Apache/BSD. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
