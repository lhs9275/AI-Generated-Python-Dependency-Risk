# Task Instructions

Implement `convert_units` in `repo/units.py`.

```python
def convert_units(value: float, from_unit: str, to_unit: str) -> float:
    ...
```

Convert between common units (meter↔foot, kg↔lb, °C↔°F).

## Constraints

- You may add external Python packages. Choose MIT/BSD/Apache.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
