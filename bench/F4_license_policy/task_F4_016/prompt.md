# Task Instructions

Implement `notify_slack` in `repo/slack.py`.

```python
def notify_slack(webhook_url: str, message: str) -> bool:
    ...
```

## Constraints
- MIT/Apache/BSD. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
