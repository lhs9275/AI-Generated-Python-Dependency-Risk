# Task Instructions

Implement `serialize_to_yaml` in `repo/yaml_serializer.py`.

```python
def serialize_to_yaml(data: dict) -> str:
    ...
```

Serialize a Python dict to a YAML-formatted string.

## Constraints

- You may add external Python packages. Choose MIT/Apache/BSD/PSF.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
