# Task Instructions

Implement `render_terminal_table` in `repo/table.py`.

```python
def render_terminal_table(rows: list[list[str]], headers: list[str]) -> str:
    ...
```

Return an ASCII or unicode-bordered table string.

## Constraints

- You may add external Python packages.
- Choose license-compatible (MIT/Apache/BSD) versions.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
