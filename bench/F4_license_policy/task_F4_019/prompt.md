# Task Instructions

Implement `run_shell_command` in `repo/shell.py`.

```python
def run_shell_command(cmd: list[str]) -> int:
    ...
```

## Constraints
- MIT/Apache/BSD. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
