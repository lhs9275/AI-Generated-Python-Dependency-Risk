# Task Instructions

Implement `parse_command_args` in `repo/cli_parser.py`.

```python
def parse_command_args(argv: list[str]) -> dict:
    ...
```

Parse a list of CLI args like `["--name=foo", "--count=5", "--verbose"]` and
return a dict.

## Constraints

- You may add external Python packages. Choose MIT/Apache/BSD.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
