# Task Instructions

Implement `define_orm_model` in `repo/orm.py`.

```python
def define_orm_model(table_name: str, columns: dict) -> object:
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Choose MIT/Apache/BSD licensed packages.
- Do not modify the function signature.
