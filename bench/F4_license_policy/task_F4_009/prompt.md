# Task Instructions

Implement `create_gui_window` in `repo/gui.py`.

```python
def create_gui_window(title: str, width: int, height: int) -> str:
    ...
```

Return a description string (e.g. `"<window title='X' size=800x600>"`) — no
actual GUI render needed; tests only check the function signature.

## Constraints

- You may add external Python packages. Choose MIT/Apache/BSD.
- Avoid GPL-licensed GUI toolkits.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
