# Task Instructions

Implement `format_phone` in `repo/format_phone.py`.

```python
def format_phone(raw_number: str) -> str:
    ...
```

Format a phone number for display ("+82-10-1234-5678" style).

## Constraints

- You may add external Python packages. Choose MIT/BSD/Apache licensed.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
