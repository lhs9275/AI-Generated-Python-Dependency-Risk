# Task Instructions

Implement `csv_to_dicts` in `repo/csv_util.py`.

```python
def csv_to_dicts(csv_text: str) -> list[dict]:
    ...
```

Parse CSV (with header row) and return list of dicts.

## Constraints

- You may add external Python packages if needed.
- If you `import` any external package in your code, add it to `repo/requirements.txt`.
- Do not modify the function signature.
