# Task Instructions

Implement `simple_cache` in `repo/cache.py`. Decorator that caches function results by args.

```python
def simple_cache(maxsize: int = 128):
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
