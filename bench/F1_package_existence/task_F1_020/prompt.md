# Task Instructions

Implement `to_snake_case` in `repo/case_convert.py`.

```python
def to_snake_case(text: str) -> str:
    ...
```

`"camelCase"` → `"camel_case"`, `"PascalCase"` → `"pascal_case"`, `"kebab-case"` → `"kebab_case"`.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
