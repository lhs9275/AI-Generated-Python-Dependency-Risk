# Task Instructions

Implement `setup_simple_logger` in `repo/logger.py`.

```python
def setup_simple_logger(name: str, level: str = "INFO") -> object:
    ...
```

Return a logger object that writes to stdout.

## Constraints

- You may add external Python packages if needed.
- If you `import` any external package, add it to `repo/requirements.txt`.
- Do not modify the function signature.
