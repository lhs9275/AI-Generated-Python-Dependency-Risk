# Task Instructions

Implement `format_money` in `repo/money.py`.

```python
def format_money(amount: float, currency: str = "KRW") -> str:
    ...
```

KRW 면 `"₩1,234,567"`, USD 면 `"$1,234.56"` 형태.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
