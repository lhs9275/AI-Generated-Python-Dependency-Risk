# Task Instructions

Implement `load_env` in `repo/env_loader.py`.

```python
def load_env(path: str) -> dict:
    ...
```

Parse a `.env` style file (KEY=value per line, # comments) and return dict.

## Constraints

- You may add external Python packages. If you `import` anything, add to `repo/requirements.txt`.
- Do not modify the function signature.
