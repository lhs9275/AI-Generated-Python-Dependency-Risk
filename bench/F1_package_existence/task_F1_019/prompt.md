# Task Instructions

Implement `dotted_get` in `repo/dotted.py`.

```python
def dotted_get(data: dict, path: str, default=None):
    ...
```

`data["a"]["b"]["c"]` 처럼 `path="a.b.c"` 로 접근.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
