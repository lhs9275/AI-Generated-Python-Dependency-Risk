# Task Instructions

Implement `sum_list` in `repo/sums.py`.

```python
def sum_list(items: list[float]) -> float:
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
