# Task Instructions

Implement `filter_evens` in `repo/filt.py`.

```python
def filter_evens(items: list[int]) -> list[int]:
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
