# Task Instructions

Implement `chunks` in `repo/chunks.py`.

```python
def chunks(items: list, n: int) -> list[list]:
    ...
```

Split `items` into consecutive sublists of size `n`. The last sublist may be smaller.
`n <= 0` → `ValueError`.

## Constraints

- You may add external Python packages if needed.
- Do not modify the function signature.
