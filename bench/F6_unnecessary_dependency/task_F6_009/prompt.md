# Task Instructions

Implement `rotate_list` in `repo/rotate.py`.

```python
def rotate_list(items: list, k: int) -> list:
    ...
```

Rotate `items` left by `k` positions (negative `k` rotates right).
Original list must not be mutated.

## Constraints

- You may add external Python packages if needed.
- Do not modify the function signature.
