# Task Instructions

Implement `merge_dicts` in `repo/dict_merge.py`.

```python
def merge_dicts(a: dict, b: dict) -> dict:
    ...
```

`b`의 값이 `a`를 덮어쓴다.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
