# Task Instructions

Implement `count_chars` in `repo/cnt.py`.

```python
def count_chars(text: str) -> int:
    ...
```

`text`의 길이 반환.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
