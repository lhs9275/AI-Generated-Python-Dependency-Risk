# Task Instructions

Implement `dedupe_preserving_order` in `repo/dedupe.py`.

```python
def dedupe_preserving_order(items: list) -> list:
    ...
```

Remove duplicates from a list while keeping the first occurrence's order.

## Constraints

- You may add external Python packages if needed.
- Do not modify the function signature.
