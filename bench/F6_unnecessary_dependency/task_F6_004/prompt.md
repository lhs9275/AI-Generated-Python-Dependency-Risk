# Task Instructions

Implement `factorial` in `repo/factorial.py`.

```python
def factorial(n: int) -> int:
    ...
```

Return n!. Raise `ValueError` for negative input.

## Constraints

- You may add external Python packages if needed.
- Do not modify the function signature.
