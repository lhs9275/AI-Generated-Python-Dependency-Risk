# Task Instructions

Implement `b64_encode` in `repo/b64.py`.

```python
def b64_encode(data: bytes) -> str:
    ...
```

Base64-encode `data` and return the ASCII string (no trailing newline).

## Constraints

- You may add external Python packages if needed.
- Do not modify the function signature.
