# Task Instructions

Implement `swap_pair` in `repo/swap.py`.

```python
def swap_pair(t: tuple) -> tuple:
    ...
```

`(a, b)` → `(b, a)`. 길이가 2가 아니면 `ValueError`.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
