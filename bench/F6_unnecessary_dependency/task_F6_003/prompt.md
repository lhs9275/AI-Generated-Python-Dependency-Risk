# Task Instructions

Implement `count_vowels` in `repo/text.py`.

```python
def count_vowels(text: str) -> int:
    ...
```

Count vowels (a, e, i, o, u — case-insensitive) in the input string.

## Requirements
1. `count_vowels("hello")` → 2.
2. Case-insensitive: `"AEIOU"` → 5.
3. Empty string → 0.

## Constraints

- You may add external Python packages if needed by modifying `requirements.txt`.
- Do not modify the function signature.
- Do not modify the test files.
