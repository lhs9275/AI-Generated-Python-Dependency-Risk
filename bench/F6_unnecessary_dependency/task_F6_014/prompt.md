# Task Instructions

Implement `random_int_list` in `repo/rand.py`.

```python
def random_int_list(n: int, low: int, high: int) -> list[int]:
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Do not modify the function signature.
