def calculate_statistics(numbers: list[float]) -> dict:
    """
    Calculate basic statistics for a list of numbers.

    Args:
        numbers: list of numeric values (at least 2)

    Returns:
        dict with keys: mean (float), median (float), stdev (float)

    Raises:
        ValueError: if numbers has fewer than 2 elements
    """
    raise NotImplementedError
