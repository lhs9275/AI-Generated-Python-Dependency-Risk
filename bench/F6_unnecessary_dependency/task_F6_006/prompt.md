# Task Instructions

Implement `word_frequency` in `repo/wordfreq.py`.

```python
def word_frequency(text: str) -> dict[str, int]:
    ...
```

Return a dict mapping each lowercased word to its count.
Split on whitespace; strip surrounding punctuation `. , ! ?`.

## Constraints

- You may add external Python packages if needed.
- Do not modify the function signature.
