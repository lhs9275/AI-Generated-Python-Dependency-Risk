# Task Instructions

Implement `log_metric` in `repo/ml_tracking.py`.

```python
def log_metric(experiment: str, name: str, value: float, step: int = 0) -> None:
    ...
```

ML experiment metric logger.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Choose currently maintained versions.
- Do not modify the function signature.
