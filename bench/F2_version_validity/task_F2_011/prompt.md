# Task Instructions

Implement `start_simple_server` in `repo/web_server.py`.

```python
def start_simple_server(handler, port: int = 8000):
    ...
```

Async web server returning handler object. Tests check signature only.

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Choose currently maintained versions.
- Do not modify the function signature.
