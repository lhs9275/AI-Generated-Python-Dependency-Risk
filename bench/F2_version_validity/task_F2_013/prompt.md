# Task Instructions

Implement `serve_wsgi` in `repo/wsgi_server.py`.

```python
def serve_wsgi(app, host: str = "0.0.0.0", port: int = 8000) -> None:
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Choose currently maintained versions.
- Do not modify the function signature.
