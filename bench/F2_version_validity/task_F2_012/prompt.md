# Task Instructions

Implement `ssh_connect` in `repo/ssh_client.py`.

```python
def ssh_connect(host: str, user: str, key_path: str) -> object:
    ...
```

## Constraints
- External package 가능. `import` 하면 requirements.txt 추가.
- Choose currently maintained versions.
- Do not modify the function signature.
